import 'package:flutter/material.dart';

class TelaVerperfilUsuario extends StatelessWidget {
  const TelaVerperfilUsuario({super.key, String? userId});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
